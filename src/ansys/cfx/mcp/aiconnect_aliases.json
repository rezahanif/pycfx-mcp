{
 "_doc": "Authored intent phrasings, merged into aiconnect-capabilities.json by scripts/gen_capabilities.py. The AiConnect gateway folds these into its BM25 search haystack only - never into what the model is shown, and never into the embedding.",
 "_rule": "An alias must not introduce a word that is COMMON in ordinary English but RARE in the corpus. BM25 IDF treats such a word as maximally discriminating, so it becomes a magnet for queries the tool should refuse. Name the OPERATION - not a material, a trade, or any everyday noun. Authored from each tool's own name and description only, never from the benchmark task prompts.",
 "_why": "Driver: aiconnector/scripts/bundle-benchmark/alias_ab.py (BUNDLE_BENCH_TARGET=pycfx).",
 "aliases": {
  "session_status": [
   "is a cfx backend attached",
   "which endpoint is connected",
   "state of the cfx leaf"
  ],
  "error_remediation": [
   "how to configure a rotating domain",
   "diagnose a solver error message",
   "walk through a cfx workflow in prose",
   "troubleshooting guidance"
  ],
  "find_api": [
   "locate the pycfx path for a setting",
   "which api path implements this action",
   "ranked candidate api paths"
  ],
  "get_help": [
   "docstring for an api path",
   "allowed values of a parameter",
   "child names under a path"
  ],
  "run_code": [
   "execute pycfx python against the session",
   "drive pre solver and post objects"
  ],
  "validate_code": [
   "dry run cfx python without side effects",
   "parse and type check before applying"
  ],
  "cfx_workflow": [
   "import a mesh file",
   "write the def file",
   "launch cfx pre",
   "lifecycle action from mesh to results"
  ],
  "cfx_model_context": [
   "compact slice of the cfx model",
   "list named objects in the case",
   "targeted context for the current setup"
  ],
  "connect_cfx": [
   "attach to a cfx session",
   "launch a cfx product instance",
   "open a case file"
  ],
  "get_setup": [
   "summary of the current cfx setup",
   "named objects and solver state"
  ],
  "set_setup": [
   "change the turbulence model",
   "assign a boundary condition value",
   "set a domain property on a path"
  ],
  "save_case": [
   "save the cfx pre case",
   "write a cfx file to disk"
  ],
  "start_solve": [
   "run the cfx solver",
   "begin the simulation from a def file",
   "submit the solve"
  ],
  "get_solve_status": [
   "is the solver still running",
   "solver run progress and results file"
  ],
  "stop_solve": [
   "halt the active solver run",
   "abort the simulation"
  ],
  "get_results": [
   "path to the res results file",
   "where the solver wrote its output"
  ],
  "disconnect_cfx": [
   "close the cfx sessions",
   "release cfx resources"
  ],
  "list_cfx_api_categories": [
   "browse cfx api categories",
   "pre solver and post api areas"
  ]
 }
}
